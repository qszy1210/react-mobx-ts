import * as React from 'react';

// import {AdvanceTree} from '@q7/athena/src/components/AdvanceTree';
import { ret } from '../allocate/run';

export function Tree(props: any) {
    const treeData = ret;
    // return (
    //     <AdvanceTree
	// 		selectable={() => true}
	// 		// onSelect={this.queryListData}
	// 		idName={"id"}
	// 		// onTreeReady={this.handleTreeReady}
	// 		data={treeData} />
    // );
    return <div></div>
}
